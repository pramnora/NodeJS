/*
 NOTE: This is the [hw01.js] module file...,
       that is called by file: [call-module-01.js];
       and, therefore, for both of these files to work, effectively;
       they both need to be kept stored together inside of the one same folder directory. 
*/       
         
exports.greetings = function (){
  return "Hello, world!";
}
